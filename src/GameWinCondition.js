import { useState } from "react";
import Button from "./Button";

const GameWinCondition = ( { player, isGameOver, gameState, clickedSquare,  }) => {

  return(
    <div>
      <Button />
    </div>
  )

};

export default GameWinCondition;